# 桌面端额外依赖（在原有后端依赖基础上追加）
# 原有后端仅需 Pillow（封面生成），无其他第三方包。

# PyInstaller — 生成内嵌 Python 运行时的独立应用
PyInstaller>=6.10,<7

# Pillow — 后端封面图和桌面图标生成
Pillow>=10,<13

# PyWebView — 将 Web 应用封装为原生桌面窗口
# macOS 自动拉取 pyobjc-framework-WebKit；Windows 使用 Edge WebView2 Runtime
pywebview>=5,<7
