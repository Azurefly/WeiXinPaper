# 公众号 AI Studio 2.1.3 发布门禁状态

## 当前判定

**Release Candidate，不能标记 Final。**

2.1.3 已继续关闭万级文章实际使用、容量证据、真实外部验证工具缺陷和平台实机验收工具缺失。真实凭证和非当前操作系统证据不得伪造。

## 已通过

- [x] 2.1.2 的 3 个 P0 与核心 P1 代码整改保持有效。
- [x] 文章中心使用服务端分页、服务端搜索、总数与 `hasMore`，不再一次加载 500 篇后在浏览器假装全量搜索。
- [x] 真实 SQLite + 真实 HTTP API 验证 10,000 篇文章。
- [x] 首页、末页、60 次随机分页和唯一关键词搜索全部成功。
- [x] 同一文章连续 100 次真实 PATCH 保存全部成功，版本历史保持最多 100 条。
- [x] 容量库 `PRAGMA integrity_check=ok`；随机分页 P95 20.62ms，保存 P95 5.99ms。
- [x] 修复真实 AI 验证脚本 `draft` 参数错误，验证链为 plan → draft → review。
- [x] 微信验证脚本支持真实封面永久素材 multipart 上传，再执行真实 `draft/add`，并支持可选清理。
- [x] 提供 Windows DPAPI 独立实机验收脚本和 CMD 入口。
- [x] 提供不依赖伪 AI 的真实 HTTP 浏览器 E2E 脚本和 CMD 入口。
- [x] 当前核心自动化 45 项通过；JavaScript 语法检查通过。

## 仍需外部环境证据

- [ ] 使用真实 AI Key 完成严格事实 plan、draft、review，并保存脱敏结果。
- [ ] 使用微信公众号测试号完成 token、真实封面永久素材上传和 `draft/add`，并在后台核对草稿。
- [ ] 在 Windows 10/11 普通用户下运行 DPAPI 验收，并补充第二账户跨用户失败证据。
- [ ] 在没有企业 `URLBlocklist=*` 的 Chrome/Edge 上运行真实服务浏览器 E2E。
- [ ] 在目标生产规格机器执行建议不少于 1 小时的持续编辑 soak；当前证据为 100 次加速连续编辑。

## Final 判定规则

上述五项均形成脱敏证据后，重新全新解压源码包和运行包，执行核心测试、容量验证、运行包自检、逐文件哈希复核，才可升级为 Final。
